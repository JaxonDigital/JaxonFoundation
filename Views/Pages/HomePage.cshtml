@using EPiServer.Core
@using EPiServer.Web.Mvc.Html
@using JaxonFoundation.Logic.ViewModels
@using JaxonFoundation.Logic.Models.Pages

@model JaxonFoundation.Logic.ViewModels.PageViewModel<HomePage>

@if (@Html.PropertyFor(m => m.Hero) != null)
    @if (@Html.PropertyFor(m => m.Hero) != null)
    {
        @Html.PropertyFor(m => m.Hero)
    }


@if (@Html.PropertyFor(m => m.MainContent) != null)
{
    @Html.PropertyFor(m => m.MainContent)
}

@if (Model.Name == "PageNotFound404")
{
    <h1>This is a 404 Not Found</h1>
}